{
  "name": "evalquake-functions",
  "version": "0.11.6",
  "private": true,
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test": "vitest run"
  },
  "dependencies": {
    "firebase-admin": "latest",
    "firebase-functions": "latest",
    "puppeteer": "latest"
  },
  "devDependencies": {
    "typescript": "latest",
    "vitest": "latest"
  }
}
